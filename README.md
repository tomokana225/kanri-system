# Class Reservation System

This is a comprehensive booking system for students to reserve classes, teachers to view schedules, and administrators to manage the platform. The system features role-based access, a dynamic calendar for booking, and push notifications.

## 🚀 Running the Project

1.  **Install dependencies:**
    ```bash
    npm install
    ```

2.  **Start the development server:**
    ```bash
    npm run dev
    ```

The application will be available at the URL provided by Vite (usually `http://localhost:5173`).